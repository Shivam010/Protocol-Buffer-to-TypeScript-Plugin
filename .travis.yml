sudo: required
language: python
services:
  - docker
script:
  - docker pull cuonglekty/alpine-python-protobuf
  - docker run cuonglekty/alpine-python-protobuf /home/travis/build/Shivam010/Protocol-Buffer-to-TypeScript-Plugin "pwd; ls -la"
  - pwd
  - chmod -R 777 .
  - ls -la
  - cd examples
  - bash ./generate.sh
  
